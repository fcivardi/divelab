---
title: "Gas Consumption"
subtitle: "From Respiratory Demand to Resource Depletion"
---

## Companion Resources

::: {.callout-tip title="Companion Resources"}
**Chapter:** 15 — Gas Consumption

**Companion notebook:** Notebook 15 — Gas Consumption

**Estimated study time:** 120–150 minutes, including the notebook

**Prerequisites:** Chapters 1, 2, and 14
:::

## Engineering Question

How do depth, time, and breathing demand deplete a finite cylinder resource?

## Learning Objectives

After completing this chapter, you should be able to:

- distinguish surface-equivalent breathing rate from ambient gas flow;
- compute depth-dependent gas demand from absolute pressure;
- formulate remaining gas as a dynamic state;
- integrate gas use over a variable depth and workload profile;
- explain why a gas forecast is conditional rather than guaranteed.

## Theory

The pressure-scaled resource model uses standard open-circuit diving gas accounting; operational reserve procedures remain deliberately outside its scope [@noaa2017; @usnavy2016].

### Surface-Equivalent Rate

Let $q_S(t)$ denote a surface-equivalent volumetric rate, often called RMV or SAC depending on convention. DiveLab uses $q_S$ in liters per minute measured at a reference surface pressure $P_0$. The equivalent surface-volume withdrawal rate at depth is

$$
q(t)=q_S(t)\frac{P_{\mathrm{amb}}(z)}{P_0}.
$$

Using the hydrostatic relation,

$$
P_{\mathrm{amb}}(z)=P_0+\rho gz.
$$

Here $P_{\mathrm{amb}}$ means ambient **absolute** pressure; it is the same physical quantity denoted by $P_{\mathrm{abs}}$ in Part I. The ambient notation is retained in Part IV to emphasize that this pressure drives respiratory and inert-gas models.

The familiar approximation $P_{\mathrm{amb}}/P_0\approx1+z/10$ is useful in seawater, but the exact pressure ratio keeps the assumptions visible.

### Gas Remaining as a State

Let $G(t)$ be the remaining surface-equivalent free-gas volume. Then

$$
\dot G(t)=-q_S(t)\frac{P_{\mathrm{amb}}(z(t))}{P_0}.
$$

For a cylinder with internal volume $V_{\mathrm{cyl}}$, the ideal-gas relation at the same reference temperature is

$$
P_{\mathrm{cyl,abs}}(t)
=P_0\frac{G(t)}{V_{\mathrm{cyl}}}.
$$

Here $P_{\mathrm{cyl,abs}}$ is absolute cylinder pressure. Gauge pressure would be $P_{\mathrm{cyl,g}}=P_{\mathrm{cyl,abs}}-P_0$ when the cylinder and gauge are at the same atmospheric reference. This approximation omits non-ideal gas behavior, temperature transients, gauge error, regulator limits, and unusable residual gas.

### Workload and Uncertainty

Write

$$
q_S(t)=q_{S,0}w(t),
$$

where $w(t)$ is a dimensionless workload multiplier. It may represent swimming effort, stress, current, thermal strain, or other demand. Because future $w(t)$ and $z(t)$ are unknown, time-to-reserve is a scenario-dependent prediction.

::: {.callout-warning title="Not a gas-planning tool"}
The equations and examples teach resource dynamics. They do not include team reserves, contingencies, gas-switch rules, regulator performance, local standards, or instructor procedures and must not be used to plan a real dive.
:::

## Worked Examples

### Example 1: Rate at 20 m

With $q_S=18~\mathrm{L/min}$ and the $10~\mathrm{m/bar}$ approximation,

$$
q(20)=18(1+20/10)=54~\mathrm{L/min}.
$$

### Example 2: A Constant-Depth Exposure

Thirty minutes at $20~\mathrm{m}$ with the same surface-equivalent rate consumes

$$
G_{\mathrm{used}}=54\times30=1620~\mathrm{L}
$$

in the idealized model.

### Example 3: Why Maximum Depth Is Insufficient

Consider two idealized ten-minute segments that both include a visit to $20~\mathrm{m}$. Profile A remains at $20~\mathrm{m}$; Profile B reaches $20~\mathrm{m}$ momentarily but spends the segment at $10~\mathrm{m}$. With $q_S=18~\mathrm{L/min}$ and the one-bar-per-ten-meters approximation,

$$
G_A=18(3)(10)=540~\mathrm{L},
\qquad
G_B\approx18(2)(10)=360~\mathrm{L}.
$$

The equal maximum depth does not imply equal resource use. In general,

$$
G_{\mathrm{used}}=\int_0^Tq_S(t)\frac{P_{\mathrm{amb}}(z(t))}{P_0}\,dt
$$

depends on the full depth and demand histories.

## DiveLab Insight

::: {.callout-important title="DiveLab Insight"}

Gas is not merely a displayed pressure. It is a monotonically decreasing resource state coupled to depth and human demand. The system is therefore dynamic even when cylinder pressure appears nearly linear over a short interval.

:::

## Engineering Perspective

Gas management resembles energy management in a battery-powered system: remaining resource is estimated, future load is uncertain, and a constraint must be respected under disturbances. A useful forecast always states its assumed future profile.

## Summary

- Ambient demand scales with absolute-pressure ratio.
- Surface-equivalent rate and ambient volumetric flow are different quantities.
- Remaining gas obeys a first-order balance law.
- Workload and depth histories jointly determine depletion.
- Operational reserve rules lie outside this educational model.

## Key Equations

| Equation | Meaning |
|:--|:--|
| $q=q_S P_{\mathrm{amb}}/P_0$ | Depth-dependent surface-equivalent withdrawal rate |
| $\dot G=-q_S P_{\mathrm{amb}}/P_0$ | Remaining-gas state equation |
| $G(T)=G(0)-\int_0^T q_S(t)P_{\mathrm{amb}}(z(t))/P_0\,dt$ | Gas balance over a variable profile |
| $P_{\mathrm{cyl,abs}}\approx P_0G/V_{\mathrm{cyl}}$ | Idealized absolute cylinder pressure at the reference temperature |

## Exercises

1. **C15-E01 · Recommended.** Compare the exact pressure ratio with $1+z/10$ from 0 to $40~\mathrm{m}$, then quantify how the approximation changes integrated gas use over one specified profile.
2. **C15-E02 · Core.** Derive the constant-depth solution for $G(t)$.
3. **C15-E03 · Core.** Construct two profiles with equal duration and maximum depth but different gas use.
4. **C15-E04 · Notebook Challenge.** Add an uncertain workload interval and report a range rather than one forecast.
5. **C15-E05 · Core.** List the assumptions required to infer free-gas volume from cylinder pressure.

**Notebook alignment.** Notebook 15 directly supports C15-E03 and C15-E04; its cylinder-volume and environmental-sensitivity experiments extend C15-E05 and C15-E01.

## Companion Notebook

Open Notebook 15 — Gas Consumption on [GitHub](https://github.com/fcivardi/divelab/blob/editorial-v2/notebooks/15_Gas_Consumption.ipynb) or [Google Colab](https://colab.research.google.com/github/fcivardi/divelab/blob/editorial-v2/notebooks/15_Gas_Consumption.ipynb) to integrate gas use over depth and workload profiles and test forecast sensitivity.

## Looking Ahead

Chapter 16 asks how a dive computer turns pressure samples into depth, vertical speed, exposure histories, alarms, and hidden-state estimates.
